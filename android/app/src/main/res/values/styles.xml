<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A tela que aparece no instante entre tocar no ícone e o site pintar.
      Fica na cor da marca para a abertura não piscar em branco.
    -->
    <style name="Theme.App" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/brand</item>
        <item name="android:statusBarColor">@color/brand</item>
    </style>
</resources>
